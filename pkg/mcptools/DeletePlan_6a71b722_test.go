// Code generated by https://github.com/flowgent-labs/mcpfather

package mcptools

import (
	"context"
	"encoding/json"
	"net/http"
	"net/http/httptest"
	"testing"

	"github.com/mark3labs/mcp-go/mcp"
	mcputils "nexus-mcp/pkg/helpers"
)

func TestNewDeletePlan_6a71b722MCPTool(t *testing.T) {
	tool := NewDeletePlan_6a71b722MCPTool()
	if tool.Name != "DeletePlan_6a71b722" {
		t.Errorf("unexpected tool name: got %q, want %q", tool.Name, "DeletePlan_6a71b722")
	}
	if tool.Description == "" {
		t.Error("tool description is empty")
	}
	if len(tool.RawInputSchema) == 0 {
		t.Error("tool input schema is empty")
	}
	var schema map[string]interface{}
	if err := json.Unmarshal(tool.RawInputSchema, &schema); err != nil {
		t.Errorf("tool input schema is not valid JSON: %v", err)
	}
	if schema["type"] != "object" {
		t.Errorf("schema type should be object, got %v", schema["type"])
	}
}

func TestDeletePlan_6a71b722Handler(t *testing.T) {
	handler := DeletePlan_6a71b722Handler
	if handler == nil {
		t.Fatal("handler is nil")
	}

	t.Run("upstream ok", func(t *testing.T) {
		ts := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
			w.Header().Set("Content-Type", "application/json")
			w.WriteHeader(http.StatusOK)
			w.Write([]byte(`{"ok":true}`))
		}))
		defer ts.Close()

		t.Setenv("MCP__UPSTREAM__ENDPOINT", ts.URL)
		mcputils.LoadConfig("nexus-mcp")

		req := mcp.CallToolRequest{
			Params: mcp.CallToolParams{
				Name:      "DeletePlan_6a71b722",
				Arguments: map[string]interface{}{},
			},
		}
		result, err := handler(context.Background(), req)
		if err != nil {
			t.Errorf("unexpected error: %v", err)
		}
		if result == nil {
			t.Fatal("result is nil")
		}
	})

	t.Run("nil args", func(t *testing.T) {
		ts := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
			w.Header().Set("Content-Type", "application/json")
			w.WriteHeader(http.StatusOK)
			w.Write([]byte(`{}`))
		}))
		defer ts.Close()

		t.Setenv("MCP__UPSTREAM__ENDPOINT", ts.URL)
		mcputils.LoadConfig("nexus-mcp")

		req := mcp.CallToolRequest{
			Params: mcp.CallToolParams{
				Name: "DeletePlan_6a71b722",
			},
		}
		result, err := handler(context.Background(), req)
		if err != nil {
			t.Errorf("unexpected error with nil args: %v", err)
		}
		if result == nil {
			t.Fatal("result is nil")
		}
	})

	t.Run("upstream error 500", func(t *testing.T) {
		ts := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
			w.WriteHeader(http.StatusInternalServerError)
			w.Write([]byte(`{"error":"internal"}`))
		}))
		defer ts.Close()

		t.Setenv("MCP__UPSTREAM__ENDPOINT", ts.URL)
		mcputils.LoadConfig("nexus-mcp")

		req := mcp.CallToolRequest{
			Params: mcp.CallToolParams{
				Name:      "DeletePlan_6a71b722",
				Arguments: map[string]interface{}{},
			},
		}
		result, err := handler(context.Background(), req)
		if err != nil {
			t.Errorf("unexpected error: %v", err)
		}
		if result == nil {
			t.Fatal("result is nil")
		}
		if !result.IsError {
			t.Error("expected error result for 500 upstream, got success")
		}
	})
}
